<Window x:Class="MiraFolio.App.Views.RecycleBinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:MiraFolio.App.Converters"
        Title="{DynamicResource RecycleWindowTitle}"
        Icon="pack://application:,,,/Resources/mirafolio-icon.ico"
        Width="900" Height="580" MinWidth="760" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
  <Window.Resources>
    <converters:ThumbnailConverter x:Key="RecycleThumbnailConverter" DecodePixelWidth="180"/>

    <Style x:Key="ActionButtonStyle" TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="Background" Value="#7C3AED"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="14,8"/>
      <Setter Property="Margin" Value="8,0,0,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                    CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.85"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.65"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.38"/>
                <Setter Property="Cursor" Value="Arrow"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
      <Setter Property="Background" Value="#B4233B"/>
    </Style>
  </Window.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <TextBlock Text="{DynamicResource RemovedImages}"
                 FontSize="22"
                 FontWeight="Bold"
                 VerticalAlignment="Center"/>
      <Button Grid.Column="1"
              Content="{DynamicResource ClearAll}"
              Style="{StaticResource DangerButtonStyle}"
              Command="{Binding ClearAllCommand}"
              ToolTip="{DynamicResource ClearAllTooltip}"/>
    </Grid>
    <TextBlock Grid.Row="1" Margin="0,7,0,18" Foreground="{StaticResource SubtextBrush}"
               Text="{DynamicResource RecycleDescription}" TextWrapping="Wrap"/>

    <Grid Grid.Row="2">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Items}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Margin="0,0,0,10" Padding="12" CornerRadius="10"
                      Background="{StaticResource SurfaceBrush}"
                      BorderBrush="#252548" BorderThickness="1">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="112"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>

                  <Border Width="100" Height="66" CornerRadius="6" ClipToBounds="True"
                          Background="#090918">
                    <Grid>
                      <Image Source="{Binding FilePath, Converter={StaticResource RecycleThumbnailConverter}}"
                             Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                      <TextBlock Text="{DynamicResource FileMissing}" FontSize="11" Foreground="#8E8CA8"
                                 HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                          <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding Exists}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </TextBlock.Style>
                      </TextBlock>
                    </Grid>
                  </Border>

                  <StackPanel Grid.Column="1" Margin="14,1,16,1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" FontSize="14"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding FilePath}" Margin="0,5,0,0" FontSize="11"
                               Foreground="{StaticResource SubtextBrush}" TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding FilePath}"/>
                    <Grid Margin="0,6,0,0">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="{Binding StatusText}" ToolTip="{Binding StatusText}"
                                 FontSize="11" Foreground="#A4A2BE"
                                 TextTrimming="CharacterEllipsis"/>
                      <TextBlock Grid.Column="1" Text="  ·  " FontSize="11" Foreground="#5F5D76"/>
                      <TextBlock Grid.Column="2" Text="{Binding RemovedAtText}" FontSize="11" Foreground="#A4A2BE"/>
                    </Grid>
                  </StackPanel>

                  <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <Button Content="{DynamicResource Restore}" Style="{StaticResource ActionButtonStyle}"
                            Margin="8,0,0,6" HorizontalAlignment="Stretch"
                            Command="{Binding DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"/>
                    <Button Content="{DynamicResource DeletePermanently}" Style="{StaticResource DangerButtonStyle}"
                            Margin="8,0,0,0" HorizontalAlignment="Stretch"
                            Command="{Binding DataContext.DeletePermanentlyCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"/>
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>

      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel.Style>
          <Style TargetType="StackPanel">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
              <DataTrigger Binding="{Binding Items.Count}" Value="0">
                <Setter Property="Visibility" Value="Visible"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </StackPanel.Style>
        <TextBlock Text="{DynamicResource RecycleEmpty}" FontSize="18" FontWeight="SemiBold"
                   HorizontalAlignment="Center"/>
        <TextBlock Text="{DynamicResource RecycleEmptyHint}"
                   Margin="0,8,0,0" Foreground="{StaticResource SubtextBrush}"
                   MaxWidth="560" TextAlignment="Center" TextWrapping="Wrap"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
